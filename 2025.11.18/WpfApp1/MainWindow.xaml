<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1" x:Class="WpfApp1.MainWindow"
        Title="Villogó Gomb Játék" Height="450" Width="350">
    <Window.Resources>
        <ControlTemplate x:Key="NoMouseOverButtonTemplate" TargetType="{x:Type Button}">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                 VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{x:Static SystemColors.ControlLightBrush}" />
                    <Setter Property="Foreground" Value="{x:Static SystemColors.GrayTextBrush}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Visibility="Hidden" x:Name="ScoreText" Grid.Row="1" Text="Score: 0" FontSize="20" Margin="10"/>
        <UniformGrid Visibility="Hidden" x:Name="GameGrid"  Rows="3" Columns="3" Margin="10">
        </UniformGrid>

        <TextBlock x:Name="tittle" Text="A 9×9-es rácson véletlenszerűen felvillan egy mező: először zöld, majd rövid idő után sárga, végül piros lesz. Kattints a zöld vagy sárga mezőre pontszerzésért. Pontozás: Zöld = +10 p, Sárga = +5 p, ha pirosra váltasz, akkor -5p" FontSize="20" Margin="10,10,-5,94" TextWrapping="Wrap" MaxWidth="300"/>
        <Button Name="startgomb" Content="Play" HorizontalAlignment="Center" Margin="0,228,0,0"  VerticalAlignment="Top" FontSize="55" Click="StartGame" Width="218" Height="79"/>
        <Slider HorizontalAlignment="Center" Margin="0,9,0,0" Minimum="0.5" Maximum="2" TickPlacement="BottomRight" Ticks="0.5,1.25,2" IsSnapToTickEnabled="True" VerticalAlignment="Top" Width="202"  ValueChanged="Difficulty" Name="diffslider" FlowDirection="RightToLeft" Value="2" Grid.Row="1"/>
        <Grid Margin="0,56,0,0" HorizontalAlignment="Stretch" Name="difftxt">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Easy" HorizontalAlignment="Center" FontSize="18" Margin="0,288,0,-288" />

            <TextBlock Grid.Column="2" Text="Hard" HorizontalAlignment="Center" FontSize="18" Margin="0,288,0,-288" />
            <TextBlock Name="HighScore" Text="High Score: 0" HorizontalAlignment="Left" FontSize="18" Margin="29,330,0,-330" Grid.ColumnSpan="2" />

        </Grid>
    </Grid>
</Window>